# react-day-picker@8 declara peers de React 16/17/18, pero el proyecto usa
# React 19 (con el que funciona correctamente). Aceptamos esa resolución de
# peers para que `npm install` corra sin flags adicionales.
legacy-peer-deps=true
